You are analyzing a narrative exchange in an interactive story to extract state changes for database updates.

Your job is to identify what ACTUALLY HAPPENED in the narration that needs to be recorded, not to speculate or invent details.

==========================================================
CRITICAL EXTRACTION RULES
==========================================================

1. **Fact Discovery Protocol**: Use factDiscoveries for ALL information changes. This is the PRIMARY method and replaces old knowledgeTransfer arrays.

2. **Registry Check**: You MUST search the FACT REGISTRY for an existing fact before creating a new one. If found, use existingFactHash. If not found, use tempId.

3. **Strict Kayfabe**: Only extract what is CLEARLY shown or stated in the Narrator's response. Do not assume or invent details.

4. **Output Format**: Respond with ONLY valid JSON. No code fences, no explanatory text. Output must start with { and end with }.

==========================================================
CURRENT DATABASE STATE
==========================================================

WORLD CONTEXT:
{world_context}

CHARACTERS:
{characters}

ACTIVE TENSIONS:
{tensions}

RECENT EVENTS:
{recent_events}

FACT REGISTRY:
{fact_registry}

==========================================================
CONVERSATION SINCE LAST EXTRACTION
==========================================================

{conversation_context}

==========================================================
EXTRACTION TASK
==========================================================

Analyze the exchange and extract changes into the following structure:

1. FACT DISCOVERIES
   - For NEW facts: Provide tempId (e.g., "discovery_1"), statement, truthValue, and allowedRevealModes.
     * allowedRevealModes: null (publicly observable) or specific modes like "OBSERVED", "TOLD", "SENSED_SPECIAL"
   - For EXISTING facts: Match to the 8-char hash in the registry using existingFactHash.
   - List every character who learned it in discoveredBy with howLearned (OBSERVED, TOLD, INFERRED, SENSED_SPECIAL, DOCUMENTED).

2. EVENTS
   - Significant actions only. Keep descriptions under 280 characters.
   - Significance levels:
     * MAJOR (plot-critical, preserved forever)
     * MINOR (important but not critical, deleted when beat ends)
     
   ⚠️ TONE-AWARE SIGNIFICANCE:
   Consider the TONE when classifying events:
   - In horror/dark genres: atmospheric changes, ominous details may be MAJOR
   - In romance: relationship moments, confessions may be MAJOR
   - In action: combat outcomes, escapes may be MAJOR
   - Match extraction granularity to the established tone
   
   ⚠️ TIME-SENSITIVE URGENCY:
   Consider TIME CONTEXT when assessing importance:
   - Events near deadlines/critical moments = higher significance
   - Reference world state for context (e.g., "full moon tonight" in werewolf story)
   
   ⚠️ RULE VIOLATIONS:
   Consider WORLD RULES when identifying special moments:
   - Rule-breaking events are typically MAJOR
   - First-time magical moments in non-magical settings = MAJOR
   - Supernatural reveals in mundane settings = MAJOR
   
3. SECRET REVELATIONS
   - Cross-reference the "doesNotKnow" lists in the character data above.
   - Update state to KNOWS (fully revealed) or SUSPICIOUS (they suspect something).
   - Keep descriptions brief and match the format in doesNotKnow lists.

4. TENSION CHANGES
   - Update pressure (1-10) for existing tensions or use CREATED for new ones.
   - Change types: ESCALATED, DE_ESCALATED, RESOLVED, CREATED
   - Keep reasons under 200 characters.
   
      ⚠️ CONTEXT-AWARE PRESSURE:
   Consider WORLD STATE and TIME when assessing new pressure levels:
   - Approaching deadlines = higher pressure (e.g., "full moon in 1 hour")
   - World state changes impact = pressure changes (e.g., "war declared" escalates existing tensions)
   - Tone influences base pressure (horror tends toward higher baseline tension)

5. CHARACTER APPEARANCES
   - APPEARED: Status changed from "potential/background" to "present"
   - LEFT: Character exited the scene
   - MENTIONED: Referenced but not physically present

==========================================================
JSON STRUCTURE
==========================================================

{
  "events": [
    {
      "description": "string",
      "significance": "MAJOR|MINOR",
      "charactersInvolved": ["string"]
    }
  ],
  "factDiscoveries": [
    {
      "tempId": "discovery_1",
      "statement": "string",
      "truthValue": true,
      "allowedRevealModes": "string or null",
      "discoveredBy": [{"characterName": "string", "howLearned": "string"}]
    },
    {
      "existingFactHash": "hash8char",
      "discoveredBy": [{"characterName": "string", "howLearned": "string"}]
    }
  ],
  "secretRevelations": [
    {
      "secretDescription": "string",
      "characterName": "string",
      "newState": "KNOWS|SUSPICIOUS",
      "howRevealed": "string"
    }
  ],
  "tensionChanges": [
    {
      "tensionDescription": "string",
      "changeType": "ESCALATED|DE_ESCALATED|RESOLVED|CREATED",
      "newPressure": 7,
      "reason": "string"
    }
  ],
  "characterAppearances": [
    {
      "characterName": "string",
      "changeType": "APPEARED|LEFT|MENTIONED",
      "context": "string"
    }
  ]
}

==========================================================
IMPORTANT NOTES
==========================================================

- If a category has no changes, use an empty array: []
- Character names must match EXACTLY with names in CHARACTER list above
- All text fields have strict length limits - stay well under to avoid truncation:
  * Event descriptions: 280 characters maximum
  * Fact statements: Keep concise (under 200 characters recommended)
  * Reasons/contexts: Under 200 characters
  * Secret/tension descriptions: Under 300 characters
- Do NOT include markdown code fences (```json) or explanations
- The response must be pure JSON that starts with { and ends with }