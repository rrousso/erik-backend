OUTPUT ONLY valid JSON with this exact structure:

{
  "setting": "brief description of where/when this takes place",
  "premise": "what is happening / core scenario",
  "userRole": "who/what the user is in this scene (PUBLIC - what characters can observe)",
  "userBackstory": "private user background (PRIVATE - characters don't know this)",
  "tone": "genre/mood/style (e.g. 'romantic', 'tense', 'horror')",
  "characters": ["character 1 name and brief description", "character 2...", ...],
  "specialRules": ["any specific rules or notes", "another rule", ...],
  "previousEvents": ["event 1", "event 2", ...]
}

**SOURCE TYPES:**

You may receive either:
- A **planning conversation** between user and Erik (for new stanzas)
- A **synopsis with exchanges** from a completed stanza (for continuing/reliving)

Extract the same JSON structure from either source.

**FIELD GUIDELINES:**

**setting**: Where/when the scene takes place

**premise**: The core scenario or situation

**userRole (PUBLIC)**: What the user APPEARS to be - what characters can observe
- "Vendor at artisan fair"
- "Servant in stepmother's household"
- "Person walking on the beach"

**userBackstory (PRIVATE)**: Background characters do NOT know unless told in-scene
- "Ex-emissary who lost their pack"
- "True heir, treated as servant"
- "Secretly a werewolf"

**tone**: Genre/mood/style (e.g., "romantic", "horror", "fairy tale")

**characters**: List of characters with brief descriptions
- Include name and role/relationship
- Do NOT include Erik (the planning assistant)
- Do NOT include the user

**specialRules**: Canon divergences, special conditions, requests
- "College AU - all characters in their 20s"
- "Post-season 3 timeline"

**previousEvents**: Chronological list of major events that happened
- Leave as empty array [] for NEW stanzas from planning conversations
- Populate with key events when extracting from synopsis/completed stanza
- Each event = one clear sentence
- Only include events that actually happened, not plans

**CRITICAL - PUBLIC VS PRIVATE:**

userRole = what characters SEE
userBackstory = what characters DON'T KNOW

If unsure, put it in userBackstory (private).

**OUTPUT:**
- ONLY the JSON object
- No preamble
- No markdown
- No explanation
- No ```json``` tags